language: java

install:
    - mvn -DpdiInstall=false -DpdiInstallDir=./target/pdi/plugins/steps/ic-riak-plugin -DskipTests=true -B -V clean

script:
    - mvn -DpdiInstall=false -DpdiInstallDir=./target/pdi/plugins/steps/ic-riak-plugin clean package

# after_success:
#    - mvn cobertura:cobertura coveralls:cobertura

deploy:
    provider: releases
    api_key: $GITHUB_OAUTH_TOKEN
    file: ./target/ic-riak-plugin-pdi-*.zip
    skip_cleanup: true
    on:
        tags: true
